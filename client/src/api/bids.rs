use crate::api::utils::get_base_url;
use crate::models::Bid;
use reqwest::{header, Client};
use std::error::Error;

pub async fn fetch_bids(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
pub async fn fetch_bid(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
pub async fn create_bid(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
pub async fn delete_bid(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
