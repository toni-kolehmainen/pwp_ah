use crate::api::utils::get_base_url;
use crate::models::Auction;
use reqwest::{header, Client};
use std::error::Error;
pub async fn fetch_auctions(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
pub async fn fetch_auction(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
pub async fn create_auction(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
pub async fn delete_auction(client: &Client) -> Result<(), Box<dyn Error>> {
    Ok(())
}
