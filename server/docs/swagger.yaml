openapi: 3.0.0
info:
  title: Users API
  version: 1.0.0
  description: API for managing users with HAL-style hypermedia responses

servers:
  - url: http://localhost:3001/api

paths:
  /ping:
    $ref: './paths/ping.yaml'
  /login/{user_id}:
    $ref: './paths/login.yaml'

components:
  schemas:
    $ref: './components/login_schemas.yaml'
  responses:
    $ref: './components/login_responses.yaml'